#include <iostream>

using namespace std ;
enum Color {Red,Green,Yellow,Blue} ;
enum Week {Sat,Sun,Mon,Tue,Wed,Thu,Fri} ;
enum Gendor {Male,Femal} ;
enum Status {Single,Married};


int main() {

    // MyColor is var with datatye Color
    Color MyColor ;


 
    return 0;
}
